<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://static.vecteezy.com/system/resources/previews/001/882/889/non_2x/cinema-film-camera-banner-with-copy-space-vector.jpg);">
  <div class="container">
    <h1>Save any kind of movies</h1>
    <p>Lorem, ipsum dolor.</p>
    <%# <a class="btn btn-flat" href="/lists/new">Añade una lista</a> %>
  </div>
</div>

<div class="my-3 mx-5">
  <h1>My Lists</h1>

  <div class="cards">
    <% @lists.each do |list| %>
      <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(https://bohmtheatre.org/wp-content/uploads/2015/09/film-banner.jpg)">
        <h3><%= link_to list.name, list_path(list) %></h3>
        <p><%= list.movies.count %></p>
      </div>

      <%# <%= link_to "Delete",
        task_path(task),
        method: :delete,
        data: {
          confirm: "Are u sure? Delete #{task.title}?"
        }
      %>

      <% end %>
  </div>

</div>

<%= link_to "Add a new list", new_list_path %>
